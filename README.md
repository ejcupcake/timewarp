# timewarp

Simple browser app that uses your camera to create a blue-line time warp (slit-scan) effect.

## Run locally

Serve the repository as static files, then open `index.html` in a browser with camera access.